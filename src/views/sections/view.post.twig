<article>
    <header>{{ post.title }}</header>
    <section>{{ post.body }}</section>
    <a href="/post/{{ post.post_id }}/edit">Edit</a>
    <div><span>Created:</span><span>{{ post.created }}</span></div>
    <div><span>Last Updated:</span><span>{{ post.updated }}</span></div>
</article>
